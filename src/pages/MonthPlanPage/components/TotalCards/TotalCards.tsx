import { moneyFormatter } from 'utils/helpers/numbers.helpers';

import React from 'react';

type Props = {};

const TotalCards = (props: Props) => {
  return <div>TotalCards</div>;
};

export default TotalCards;
